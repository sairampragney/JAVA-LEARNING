# Sum of First N Natural Numbers

A simple Java program that calculates the **sum of the first `n` natural numbers** using a `for` loop.

---

## 📌 About

This program reads a positive integer `n` from the user and calculates the sum of all natural numbers from `1` to `n`.

For example, if:

```text
n = 5
